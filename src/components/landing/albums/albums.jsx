import React from 'react';

import './albums.css';

const Albums = () => (
  <div className="albums">
    <div className="albums-title">
      <h1>ALBUMS</h1>
    </div>
  </div>
);

export default Albums;
